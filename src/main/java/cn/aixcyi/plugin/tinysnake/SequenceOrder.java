package cn.aixcyi.plugin.tinysnake;

public enum SequenceOrder {
    /**
     * 按符号出现顺序排序。
     */
    APPEARANCE,

    /**
     * 按字母先后顺序排序（不区分大小写）。
     */
    ALPHABET,

    /**
     * 按字母先后顺序排序（区分大小写）。
     */
    CHARSET
}
